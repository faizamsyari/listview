import 'package:flutter/material.dart';

class PageChat extends StatelessWidget {
  const PageChat({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text("Page Chat"),
        centerTitle: false,
      ),
    );
  }
}
